Since these PHP code files has html content in the print lines and echo commands, best would be to copy this folder under the htdocs folder of the apache webserver. 
To set up PHP, apache web server and database for learning, install xampp.
